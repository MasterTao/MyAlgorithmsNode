package com.monkey.binarysearch;

/**
 * 在一个有序数组中，找>=某个数最左侧的位置
 *
 * @author tao
 * @date 2021/7/8 5:34 下午
 */
public class NearestLeftNum {

    public static int findNearestLeftNum(int[] arr, int target) {

        if (arr == null || arr.length == 0) {
            return -1;
        }

        // 边界条件
        // 如果target的值比arr里的每个数都大，


        return -1;
    }



}
